VoiceAITranslator RC18 - Gradle Wrapper Preparation

Purpose:
Fix GitHub Actions error:
./gradlew: No such file or directory

Included:
- gradle wrapper folder structure
- build workflow update
- Android project files

Note:
gradle-wrapper.jar is a binary file generated by Gradle and must be added from a real Gradle wrapper generation step.
